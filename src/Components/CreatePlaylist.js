export default function CreatePlaylist() {
    //will have a few sliders to filter songs

    //will have a search bar so that you
    //can also filter using a selected song
    
    return (
        <div className="create">
            hello
        </div>
    );
  }